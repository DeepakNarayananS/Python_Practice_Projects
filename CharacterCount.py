# Simple Python code snippet that counts the number of characters in a given sentence
def count_characters(sentence):
    return len(sentence)

# Usage
user_input = input("Enter a sentence: ")
character_count = count_characters(user_input)
print(f"The number of characters in the sentence is: {character_count}")

